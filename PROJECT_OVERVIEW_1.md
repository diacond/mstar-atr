# 이 프로젝트, 쉽게 설명하면

이 문서는 `mstar-atr` 프로젝트를 배터리나 SAR 지식이 전혀 없어도
이해할 수 있도록 쉬운 말로 풀어 쓴 설명입니다. 코드와 숫자의 출처는
전부 `README.md`에 있고, 이 문서는 "왜 이걸 만들었고, 뭘 하는
프로젝트고, 어떤 시행착오를 거쳐 지금 모습이 됐는지"를 이야기로
풀어놓은 것입니다.

## 1. 왜 이 주제를 골랐는가

배터리 프로젝트(`battery-soh-pipeline`)가 "센서 데이터로 숫자를
예측하는" 회귀 문제였다면, 이번엔 "이미지를 보고 종류를 맞히는"
분류 문제를 다뤄보고 싶었습니다. 그중에서도 흔한 고양이/개 분류
대신 SAR(합성개구레이더) 영상을 고른 이유는 두 가지입니다.

첫째, SAR 영상은 우리가 평소에 보는 사진과 완전히 다릅니다. 색깔이
없고, 같은 물체도 찍는 각도에 따라 다르게 보이고, 화면 전체에
지지직거리는 노이즈(스페클 노이즈)가 깔려 있습니다. "사람 눈에도
어려운 문제를 기계는 어떻게 푸는가"를 다뤄보고 싶었습니다.

둘째, 이 데이터셋(MSTAR)은 미국 국방부 산하 연구소가 공개한, SAR
표적 인식 분야에서 가장 유명한 표준 벤치마크입니다. 즉 "적당히
쉬운 장난감 데이터"가 아니라, 실제 이 분야 연구자들이 서로 성능을
비교할 때 쓰는 데이터로 직접 실험해볼 수 있었습니다.

## 2. 목적 — 무엇을 만들려고 했는가

목표는 단순합니다. **레이더로 찍은 흑백 영상 한 장을 보고, 그 안의
차량이 10종류(전차 2종, 장갑차 4종, 자주포, 대공차량, 트럭,
불도저) 중 무엇인지 자동으로 맞히는 프로그램을 만드는 것**입니다.

실제 정찰 상황을 생각하면, 레이더가 찍어온 영상을 사람이 한 장 한
장 눈으로 판독하는 건 시간이 오래 걸립니다. 이 프로젝트는 그 1차
판독을 기계가 먼저 해주고, 사람은 결과를 확인만 하면 되는 구조를
작게 재현한 것입니다. 그래서 최종 결과물도 "이미지를 넣으면
`{예측 클래스, 확신도, 클래스별 확률}`을 돌려주는 API 서버"
형태로 만들었습니다.

## 3. 가설 — 만들면서 세웠던 예상들

이 프로젝트는 하나의 큰 가설이 아니라, 진행하면서 부딪힌 갈림길마다
작은 가설을 세우고 실측으로 확인하는 방식으로 진행했습니다.

**가설 A. "영상을 더 크게 잘라서 더 많은 정보를 주면 정확도가 더
좋아질 것이다."**
처음엔 당연히 그럴 거라 생각하고 원본 영상 중앙을 128×128 크기로
크게 잘라서 썼습니다.

**가설 B. "트리 기반 모델(RandomForest)이 배터리 프로젝트에서 잘
됐으니, 여기서도 잘 될 것이다."**
배터리 프로젝트에서 RandomForest로 좋은 결과를 봤기 때문에, 이미지
특징을 추출한 뒤에도 같은 모델을 먼저 시도했습니다.

두 가설 모두 아래에서 보듯 **틀린 것으로 확인**됐고, 그 이유를
추적한 과정이 이 프로젝트에서 가장 흥미로운 부분입니다.

## 4. 내용 — 실제로 어떻게 만들었는가

### (1) 데이터

MSTAR 데이터셋 중 가장 널리 쓰이는 "10-클래스 표준 조건(SOC)" 버전을
사용했습니다. 흥미로운 점은, 이 데이터셋 자체가 이미 "다른 조건에서
찍은 사진으로 시험한다"는 원칙을 내장하고 있다는 것입니다 — 학습용
사진은 카메라(레이더)를 17도 각도로 내려다보고 찍은 것이고, 시험용
사진은 15도 각도로 찍은, 완전히 다른 촬영 회차의 사진입니다. 즉
"학습 때 본 적 없는 조건에서 시험한다"는 배터리 프로젝트의 LOBO
검증과 같은 정신이 데이터셋 구조 자체에 이미 들어있습니다. (학습
2,746장, 시험 2,425장)

### (2) 파이프라인 구조

```
원본 SAR 영상(JPEG)
   → 중앙을 정사각형으로 자르기(크롭)
   → HOG(방향 기울기 히스토그램) 특징 추출 — "이 영상의 윤곽선이
     어느 방향으로 얼마나 두드러지는가"를 숫자로 표현
   → 표준화(StandardScaler) + SVM(서포트 벡터 머신) 분류기 학습
   → 저장된 모델로 새 이미지를 분류하는 API 서버
```

여기서 "HOG"라는 특징 추출 방법은 딥러닝(CNN)이 널리 쓰이기 전부터
있던 고전적인 컴퓨터 비전 기법입니다. 영상을 잘게 나눠서 각 조각의
가장자리(edge)가 어느 방향을 향하는지 히스토그램으로 요약합니다.
사람으로 치면 "이 실루엣의 윤곽이 대략 이런 모양이다"를 숫자로
바꿔주는 셈입니다.

### (3) 왜 딥러닝(CNN)이 아니라 고전적 방법(HOG+SVM)부터 시도했는가

요즘은 이미지 분류라고 하면 CNN 같은 딥러닝을 먼저 떠올리기 쉽지만,
일부러 더 단순하고 해석하기 쉬운 방법부터 시작했습니다. 이유는
두 가지입니다: 데이터가 5천 장 남짓으로 딥러닝을 학습시키기엔
많지 않은 규모라는 점, 그리고 "왜 이 클래스로 분류했는지"를 특징
단위로 뜯어보기가 고전적 방법에서 훨씬 쉽다는 점입니다. 무거운
방법을 바로 쓰기 전에, 가벼운 방법으로 어디까지 가능한지 먼저
확인해보고 싶었습니다.

### (4) 가설 A가 틀렸다는 걸 어떻게 발견했는가

128×128로 크게 잘라서 학습한 첫 모델의 정확도는 78%였습니다.
나쁘지 않다고 생각할 수도 있지만, "혹시 자르는 크기를 바꾸면 더
좋아지지 않을까?"라는 의문에서 64, 80, 96, 128 네 가지 크기를
전부 비교해봤습니다. 결과는 예상과 정반대였습니다 — **크기를 줄일수록
오히려 정확도가 올라가서, 가장 작은 64×64에서 87%로 가장 높았습니다.**

원인은 SAR 영상의 특성에 있었습니다. SAR 사진은 표적 주변에
배경 잡음(클러터)과 스페클 노이즈가 넓게 깔려 있는데, 크게 자를수록
차량의 실제 형태보다 이 노이즈 영역을 더 많이 담게 됩니다. 즉
"정보를 더 준다고 항상 도움이 되는 게 아니라, 노이즈까지 같이
많이 주면 오히려 방해가 된다"는 것을 확인한 것입니다. (배터리
프로젝트에서 새 피처를 추가했더니 일부 배터리의 잔여 수명 예측이
오히려 나빠졌던 것과 같은 종류의 교훈입니다 — 더 많은 정보 ≠
항상 더 좋은 결과.)

### (5) 가설 B가 틀렸다는 걸 어떻게 발견했는가

같은 HOG 특징으로 RandomForest를 먼저 시도했더니 정확도가 60%에
그쳤습니다. HOG 특징은 1,764개의 숫자로 이뤄진, 미세한 방향 차이가
중요한 연속적인 값인데, RandomForest 같은 트리 기반 모델은 "이
값이 얼마보다 큰가/작은가"로 딱딱 잘라서 판단하는 방식이라 이런
미세한 방향 차이를 잘 살리지 못하는 것으로 보였습니다. 대신 모든
특징의 스케일을 맞춘(표준화) 뒤 SVM(서포트 벡터 머신)이라는, 이런
연속적 고차원 데이터에 강한 분류기를 썼더니 87%까지 올라갔고, 이
조합으로 최종 확정했습니다.

## 5. 결과

최종 모델을 "촬영 각도가 다른, 학습 때 전혀 보지 않은 시험 사진"
2,425장에 적용한 정확도는 **86.7%**입니다. 10개 클래스 각각의
정확도도 크게 튀는 것 없이 78~96% 사이에 고르게 분포합니다.

혼동행렬을 들여다보면 우연이 아닌 패턴이 보입니다. 가장 자주
헷갈리는 두 쌍은 **BTR60↔BTR70**과 **2S1↔T62**입니다. BTR60과
BTR70은 둘 다 옛 소련제 8x8 바퀴형 장갑차로 실제로 외형이 거의
동일해서, SAR 표적 인식 연구에서도 유명하게 "구분하기 어려운 쌍"으로
꼽힙니다. 2S1(자주포)과 T62(전차)도 둘 다 궤도형 차량에 크기·형태가
비슷해 레이더에 비치는 모습이 닮았습니다. 이건 모델이 부족해서가
아니라,애초에 실루엣이 비슷한 차량들이라는 물리적인 한계에 가깝습니다
— 광학 사진이었다면 도색이나 세부 디테일로 구분했을 것을, SAR은
형태 정보만으로 구분해야 하기 때문입니다.

이 결과는 배터리 프로젝트와 마찬가지로 "그럴듯한 예상이 항상
맞지는 않는다"는 걸 두 번이나 확인시켜준 프로젝트였습니다 — 크게
자르면 좋을 거라는 예상, 트리 모델이 잘 될 거라는 예상이 둘 다
빗나갔고, 그 이유를 추적해서 고친 결과가 지금의 87%입니다.

## 6. 한계와 다음 단계

이 프로젝트도 배터리 프로젝트처럼 스스로 한계를 인정하고 기록해
두었습니다.

- 아직 CNN(딥러닝) 방식과 정확도·학습 시간을 직접 비교해보지
  않았습니다. HOG+SVM으로 87%가 나왔지만, 딥러닝을 쓰면 얼마나
  더 좋아지는지, 그 차이가 실제로 감수할 만한 수준인지는 다음 과제로
  남아있습니다.
- BTR60/BTR70처럼 실루엣 자체가 비슷한 차량은 한 장의 영상만으로는
  근본적인 한계가 있어 보입니다. 여러 관측각의 영상을 함께 보는
  방식으로 개선할 여지가 있는지는 아직 검토하지 않았습니다.
- 지금은 확신도가 낮은 예측도 다른 예측과 똑같이 하나의 답으로
  돌려줍니다. 실제 서비스라면 "확신도가 낮으면 사람이 재검토"하는
  후속 처리가 필요할 텐데, 이 정책은 아직 추가하지 않았습니다.

## 7. 이 프로젝트로 보여줄 수 있는 것

- 딥러닝이 아니어도, 데이터 규모와 문제 특성에 맞는 고전적 방법으로
  먼저 베이스라인을 잡고 판단 근거를 남기는 태도
- "정보를 더 준다고 항상 좋아지는 건 아니다"를 크롭 크기 실험으로
  직접 수치로 확인하고, 원인(배경 노이즈)까지 추적하는 과정
- 모델이 헷갈리는 지점을 그냥 넘기지 않고, 혼동행렬을 열어 "왜
  헷갈리는가"를 실제 차량의 외형적 유사성과 연결해 설명하는 습관
  — 이건 배터리 프로젝트에서 "검증됐다는 결과를 그대로 믿지 않고
  원인을 파헤치는" 태도와 같은 맥락입니다.
