# 프로젝트 개요 (쉽게 풀어쓴 설명)

SAR 지식 없이도 이 프로젝트가 무엇을 하고 어떤 시행착오를 거쳤는지 이해할 수 있도록 정리한 문서다. 수치와 코드는 [README.md](./README.md)를 참고한다.

## 왜 이 주제인가

- 수치를 예측하는 회귀 문제에 이어, 이미지를 보고 종류를 맞히는 분류 문제를 다뤄 보고 싶었다.
- SAR(합성개구레이더) 영상은 색이 없고, 찍는 각도에 따라 모양이 달라지며, 노이즈가 많아 사람이 보기에도 어렵다.
- MSTAR는 이 분야 연구자들이 성능을 비교할 때 쓰는 표준 벤치마크라, 실제 연구와 같은 조건에서 실험할 수 있다.

## 무엇을 만들었나

레이더 영상 한 장을 넣으면 차량 10종(전차, 장갑차, 자주포, 대공차량, 트럭, 불도저) 중 무엇인지 맞히는 API 서버다. 결과로 예측 클래스, 확신도, 클래스별 확률을 돌려준다. 사람이 영상을 한 장씩 판독하기 전에 기계가 1차 분류를 해 주는 구조를 작게 구현했다.

## 어떻게 만들었나

```
레이더 영상(JPEG)
  → 가운데를 정사각형으로 자르기
  → HOG 특징 추출: 윤곽선이 어느 방향으로 얼마나 두드러지는지를 숫자로 요약
  → 표준화 + SVM 분류기 학습
  → 학습된 모델로 새 영상을 분류하는 API
```

- **데이터:** 학습용은 17°, 시험용은 15° 각도에서 찍은 서로 다른 영상이다. 학습 때 보지 못한 조건에서 시험하게 된다.
- **딥러닝을 바로 쓰지 않은 이유:** 데이터가 5천 장 남짓으로 많지 않고, 가벼운 방법이 어디까지 되는지 먼저 확인하고 싶었다. 고전적 방법은 어떤 특징 때문에 헷갈리는지 분석하기도 쉽다.

## 틀린 예상 두 가지

**1. "크게 자르면 정보가 많아져 더 잘 맞힐 것이다" → 틀렸다.**
128x128로 자르면 정확도가 78%였다. 64, 80, 96, 128을 비교하니 작을수록 좋아져 64x64에서 87%가 나왔다. SAR 영상은 표적 주변에 배경 잡음이 넓게 깔려 있어, 크게 자를수록 차량보다 잡음을 더 많이 담는다.

**2. "트리 모델(RandomForest)도 잘 될 것이다" → 틀렸다.**
정확도가 60%에 그쳤다. HOG 특징은 1,764개의 연속적인 숫자이고 미세한 방향 차이가 중요한데, 트리 모델은 "값이 얼마보다 큰가"로 잘라 판단해 이런 차이를 잘 살리지 못한다. 특징을 표준화한 뒤 SVM을 쓰자 87%까지 올라갔다.

## 결과

- 시험 영상 2,425장에서 정확도 **86.7%**, 클래스별 정확도는 78~96%다.
- 가장 많이 헷갈린 쌍은 **BTR60 ↔ BTR70**, **2S1 ↔ T62**다.
  - BTR60과 BTR70은 외형이 거의 같은 바퀴형 장갑차다.
  - 2S1(자주포)과 T62(전차)는 크기와 형태가 비슷한 궤도형 차량이다.
  - 사진이라면 도색이나 세부 디테일로 구분하겠지만, SAR은 형태만으로 구분해야 한다.

## 한계와 다음 단계

- CNN과 정확도·학습 시간을 아직 비교하지 않았다.
- 실루엣이 비슷한 차량은 영상 한 장으로는 한계가 있다. 여러 각도의 영상을 함께 쓰는 방법은 아직 검토하지 않았다.
- 확신도가 낮은 예측도 다른 예측과 똑같이 반환한다. 실제 서비스라면 사람이 재검토하는 절차가 필요하다.
